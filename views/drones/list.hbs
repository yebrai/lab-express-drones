<h2>Available drones</h2>
{{#each response}}
    <p>Modelo: <a href="/drones/{{_id}}/edit">{{name}}</a></p>
    <p>Propulsores: {{propellers}}</p>
    <p>Velocidad: {{maxSpeed}}</p>

    <form action="/drones/{{_id}}/delete" method="POST">
    <button>Delete</button>
    </form>

{{/each}}